{% if section.settings.nosto_id != blank %}
  <div class="homepage-section index-section" data-section-id="{{ section.id }}">
    <div class="row homepage-section">
      {% include 'nosto-element' with section.settings.nosto_id %}
    </div>
  </div>
{% endif %}

{% schema %}
  {
    "name": "Nosto element",
    "settings": [
      {
        "type": "text",
        "label": "Nosto ID",
        "id": "nosto_id",
        "default": "frontpage-nosto-2"
      }
    ],
    "presets": [{
      "name": "Nosto element",
      "category": "Collection"
    }]
  }
{% endschema %}
